// This file is automatically generated.
#pragma once
#include <nvneural/CoreTypes.h>
#include <nvneural/RefPtr.h>

// Forward-declare what we can inside nvneural
namespace nvneural {
    class INetwork;
    class ClassRegistry;
} // namespace nvneural

/// Construct the layers corresponding to OIDN inside a new network.
///
/// \param pNetwork Network to build
/// \param registry Class registry able to construct the needed layer objects
nvneural::NeuralResult BuildNetwork_OIDN(nvneural::RefPtr<nvneural::INetwork> pNetwork, const nvneural::IClassRegistry* pRegistry);


